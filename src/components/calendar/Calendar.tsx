import React from 'react';
import styles from './Calendar.module.css';

const Calendar = () => {
  return <article className={styles['calendar-container']}>test</article>;
};

export default Calendar;
